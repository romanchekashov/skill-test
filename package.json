{
  "private": true,
  "name": "skill-test",
  "workspaces": [
    "data",
    "server",
    "web",
    "client"
  ],
  "scripts": {
    "start-server": "yarn workspace @skill-test/server run start",
    "start-web": "yarn workspace @skill-test/web run start",
    "start-client": "yarn workspace @skill-test/client run dev",
    "test": "yarn workspaces run test"
  }
}
